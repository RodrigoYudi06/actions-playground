name: workflow exercio 2

on:
  push:
   
jobs:
  
  checkout_code:
    name: Faz checkout do name
    runs-on: ubuntu-latest
   
  steps:
    - name: Checkout repositorio
      uses: actions/checkout@v3.3.0
  
    -name: Upload artefato
     uses: actions/upload-artifact@v3.1.2
     with:
      name: ${{ vars.NOME }}
      path: .
